<%= form_for(@user) do |f| %>

  
<%= render 'shared/error_messages',object: f.object %>


  名前:
  
<%= f.text_field :name %>


  Eメールアドレス:
  
<%= f.text_field :email %>

  年齢:
<%= f.text_field :age %>

  都道府県:
<%= f.text_field :area %>

  
<%= f.submit class: "btn btn-sm btn-primary" %>

<% end %>
